# a variable has a type, size, name, adress, value
# Casting changing a variable value from float or Int or something else
# print("hello") does not have value so it cannot be stored in a variable
# mathematic equations if the operation is equal it is read left to right
# \t tab space \b backspace \n new line \\ converges on \ into one

